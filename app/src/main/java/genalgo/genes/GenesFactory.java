package genalgo.genes;

public class GenesFactory {
    public static Gene getRandomGene() {
        // This method should return a random Gene instance.
        // For now, we will return a new Gene instance.
        return new Gene();
    }
}
